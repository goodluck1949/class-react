import { combineReducers } from "redux";
function xxx(preventDefault = {}, action) {
  switch (action.type) {
    default:
      return preventDefault;
  }
}

export default combineReducers({ xxx });
